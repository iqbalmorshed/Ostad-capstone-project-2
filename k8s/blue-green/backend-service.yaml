apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: quickhire
spec:
  type: NodePort
  selector:
    app: backend
    version: blue
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30021
