apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quickhire
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/iqbalmorshed/Ostad-capstone-project-2.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: quickhire
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
